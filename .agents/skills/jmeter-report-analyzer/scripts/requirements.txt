# JMeter Loader Skills - Python Dependencies
# 安装命令: pip install -r requirements.txt

# 核心依赖
jinja2>=3.0.0           # 用于模板渲染 (generate_jmx.py 核心功能)

# 可选依赖 - 用于高级功能
pandas>=1.0.0           # 用于高级数据处理 (parse_jtl.py 可选)
matplotlib>=3.0.0       # 用于生成图表 (parse_jtl.py 可选)

# 开发和测试依赖 (可选)
pytest>=6.0.0            # 单元测试
pytest-cov>=2.0.0        # 覆盖率测试
black>=21.0.0            # 代码格式化
